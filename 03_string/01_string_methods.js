let name = "mohd sameer";
const length = name.length;
const slice = name.slice(0, 4);
const upperCaseName = name.toUpperCase();
const lowerCaseName = name.toLowerCase();
const conCatString = name.concat(" ", "is a mern developer");
const indexOf = name.indexOf("m");
const lastIndexOf = name.lastIndexOf("m");
const includesString = name.includes("sameer");
const startsWith = name.startsWith("mohd");
const endsWith = name.endsWith("sameer");
const replaceName = name.replace("mohd", "shaik");
const replaceAllName = name.replaceAll("e", "3");
const splitName = name.split("")
const charAtName = name.charAt(2);
const charCodeAt = name.charCodeAt(2);

console.log(charCodeAt);
